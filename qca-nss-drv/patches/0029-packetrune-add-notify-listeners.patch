diff --git a/exports/nss_ipv4.h b/exports/nss_ipv4.h
index db33cfb..99bffba 100644
--- a/exports/nss_ipv4.h
+++ b/exports/nss_ipv4.h
@@ -1118,6 +1118,22 @@ extern nss_tx_status_t nss_ipv4_tx_with_size(struct nss_ctx_instance *nss_ctx, s
  */
 extern struct nss_ctx_instance *nss_ipv4_notify_register(nss_ipv4_msg_callback_t cb, void *app_data);
 
+/**
+ * nss_ipv4_notify_register_listener
+ *	Registers an additional IPv4 notifier callback without replacing the
+ *	legacy single-slot callback.
+ *
+ * @datatypes
+ * nss_ipv4_msg_callback_t
+ *
+ * @param[in] cb        Callback function for the message.
+ * @param[in] app_data  Pointer to the application context of the message.
+ *
+ * @return
+ * 0 on success or a negative error code if no listener slots are available.
+ */
+extern int nss_ipv4_notify_register_listener(nss_ipv4_msg_callback_t cb, void *app_data);
+
 /**
  * nss_ipv4_notify_unregister
  *	Degisters an IPv4 message notifier callback from the NSS.
@@ -1130,6 +1146,16 @@ extern struct nss_ctx_instance *nss_ipv4_notify_register(nss_ipv4_msg_callback_t
  */
 extern void nss_ipv4_notify_unregister(void);
 
+/**
+ * nss_ipv4_notify_unregister_listener
+ *	Deregisters an additional IPv4 notifier callback that was registered
+ *	with nss_ipv4_notify_register_listener().
+ *
+ * @param[in] cb        Callback function for the message.
+ * @param[in] app_data  Pointer to the application context of the message.
+ */
+extern void nss_ipv4_notify_unregister_listener(nss_ipv4_msg_callback_t cb, void *app_data);
+
 /**
  * nss_ipv4_conn_sync_many_notify_register
  *	Registers a notifier callback with the NSS for connection synchronization
@@ -1145,6 +1171,18 @@ extern void nss_ipv4_notify_unregister(void);
  */
 extern void nss_ipv4_conn_sync_many_notify_register(nss_ipv4_msg_callback_t cb);
 
+/**
+ * nss_ipv4_conn_sync_many_notify_register_listener
+ *	Registers an additional conn_sync_many callback without replacing the
+ *	legacy single-slot callback.
+ *
+ * @param[in] cb  Callback function for the message.
+ *
+ * @return
+ * 0 on success or a negative error code if no listener slots are available.
+ */
+extern int nss_ipv4_conn_sync_many_notify_register_listener(nss_ipv4_msg_callback_t cb);
+
 /**
  * nss_ipv4_conn_sync_many_notify_unregister
  *	Degisters a connection synchronization notifier callback from the NSS.
@@ -1157,6 +1195,15 @@ extern void nss_ipv4_conn_sync_many_notify_register(nss_ipv4_msg_callback_t cb);
  */
 extern void nss_ipv4_conn_sync_many_notify_unregister(void);
 
+/**
+ * nss_ipv4_conn_sync_many_notify_unregister_listener
+ *	Deregisters an additional conn_sync_many callback that was registered
+ *	with nss_ipv4_conn_sync_many_notify_register_listener().
+ *
+ * @param[in] cb  Callback function for the message.
+ */
+extern void nss_ipv4_conn_sync_many_notify_unregister_listener(nss_ipv4_msg_callback_t cb);
+
 /**
  * nss_ipv4_get_mgr
  *	Gets the NSS context that is managing IPv4 processes.
diff --git a/exports/nss_ipv6.h b/exports/nss_ipv6.h
index 0465722..e62e13b 100644
--- a/exports/nss_ipv6.h
+++ b/exports/nss_ipv6.h
@@ -1111,6 +1111,22 @@ extern nss_tx_status_t nss_ipv6_tx_with_size(struct nss_ctx_instance *nss_ctx, s
  */
 extern struct nss_ctx_instance *nss_ipv6_notify_register(nss_ipv6_msg_callback_t cb, void *app_data);
 
+/**
+ * nss_ipv6_notify_register_listener
+ *	Registers an additional IPv6 notifier callback without replacing the
+ *	legacy single-slot callback.
+ *
+ * @datatypes
+ * nss_ipv6_msg_callback_t
+ *
+ * @param[in] cb        Callback function for the message.
+ * @param[in] app_data  Pointer to the application context of the message.
+ *
+ * @return
+ * 0 on success or a negative error code if no listener slots are available.
+ */
+extern int nss_ipv6_notify_register_listener(nss_ipv6_msg_callback_t cb, void *app_data);
+
 /**
  * nss_ipv6_notify_unregister
  *	Deregisters a notifier callback from the NSS.
@@ -1123,6 +1139,16 @@ extern struct nss_ctx_instance *nss_ipv6_notify_register(nss_ipv6_msg_callback_t
  */
 extern void nss_ipv6_notify_unregister(void);
 
+/**
+ * nss_ipv6_notify_unregister_listener
+ *	Deregisters an additional IPv6 notifier callback that was registered
+ *	with nss_ipv6_notify_register_listener().
+ *
+ * @param[in] cb        Callback function for the message.
+ * @param[in] app_data  Pointer to the application context of the message.
+ */
+extern void nss_ipv6_notify_unregister_listener(nss_ipv6_msg_callback_t cb, void *app_data);
+
 /**
  * nss_ipv6_conn_sync_many_notify_register
  *	Registers a notifier callback with the NSS for connection synchronization
@@ -1138,6 +1164,18 @@ extern void nss_ipv6_notify_unregister(void);
  */
 extern void nss_ipv6_conn_sync_many_notify_register(nss_ipv6_msg_callback_t cb);
 
+/**
+ * nss_ipv6_conn_sync_many_notify_register_listener
+ *	Registers an additional conn_sync_many callback without replacing the
+ *	legacy single-slot callback.
+ *
+ * @param[in] cb  Callback function for the message.
+ *
+ * @return
+ * 0 on success or a negative error code if no listener slots are available.
+ */
+extern int nss_ipv6_conn_sync_many_notify_register_listener(nss_ipv6_msg_callback_t cb);
+
 /**
  * nss_ipv6_conn_sync_many_notify_unregister
  *	Degisters a notifier callback from the NSS.
@@ -1150,6 +1188,15 @@ extern void nss_ipv6_conn_sync_many_notify_register(nss_ipv6_msg_callback_t cb);
  */
 extern void nss_ipv6_conn_sync_many_notify_unregister(void);
 
+/**
+ * nss_ipv6_conn_sync_many_notify_unregister_listener
+ *	Deregisters an additional conn_sync_many callback that was registered
+ *	with nss_ipv6_conn_sync_many_notify_register_listener().
+ *
+ * @param[in] cb  Callback function for the message.
+ */
+extern void nss_ipv6_conn_sync_many_notify_unregister_listener(nss_ipv6_msg_callback_t cb);
+
 /**
  * nss_ipv6_get_mgr
  *	Gets the NSS context that is managing the IPv6 processes.
diff --git a/nss_ipv4.c b/nss_ipv4.c
index 4ce6c38..6885da8 100644
--- a/nss_ipv4.c
+++ b/nss_ipv4.c
@@ -56,6 +56,17 @@ static struct nss_dscp_map_entry mapping[NSS_DSCP_MAP_ARRAY_SIZE];
  */
 nss_ipv4_msg_callback_t nss_ipv4_conn_sync_many_msg_cb = NULL;
 
+#define NSS_IPV4_NOTIFY_LISTENER_MAX 8
+
+struct nss_ipv4_notify_listener {
+	nss_ipv4_msg_callback_t cb;
+	void *app_data;
+};
+
+static DEFINE_SPINLOCK(nss_ipv4_notify_listener_lock);
+static struct nss_ipv4_notify_listener nss_ipv4_notify_listeners[NSS_IPV4_NOTIFY_LISTENER_MAX];
+static nss_ipv4_msg_callback_t nss_ipv4_conn_sync_many_listeners[NSS_IPV4_NOTIFY_LISTENER_MAX];
+
 /*
  * nss_ipv4_dscp_map_usage()
  *	Help function shows the usage of the command.
@@ -87,6 +98,42 @@ static uint32_t nss_ipv4_get_total_conn_count(int ipv4_num_conn)
  * nss_ipv4_rx_msg_handler()
  *	Handle NSS -> HLOS messages for IPv4 bridge/route
  */
+static void nss_ipv4_notify_listener_dispatch(struct nss_ipv4_msg *nim)
+{
+	struct nss_ipv4_notify_listener listeners[NSS_IPV4_NOTIFY_LISTENER_MAX];
+	unsigned long flags;
+	int i, count = 0;
+
+	spin_lock_irqsave(&nss_ipv4_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV4_NOTIFY_LISTENER_MAX; i++) {
+		if (!nss_ipv4_notify_listeners[i].cb)
+			continue;
+		listeners[count++] = nss_ipv4_notify_listeners[i];
+	}
+	spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+
+	for (i = 0; i < count; i++)
+		listeners[i].cb(listeners[i].app_data, nim);
+}
+
+static void nss_ipv4_conn_sync_many_listener_dispatch(struct nss_ipv4_msg *nim)
+{
+	nss_ipv4_msg_callback_t listeners[NSS_IPV4_NOTIFY_LISTENER_MAX];
+	unsigned long flags;
+	int i, count = 0;
+
+	spin_lock_irqsave(&nss_ipv4_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV4_NOTIFY_LISTENER_MAX; i++) {
+		if (!nss_ipv4_conn_sync_many_listeners[i])
+			continue;
+		listeners[count++] = nss_ipv4_conn_sync_many_listeners[i];
+	}
+	spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+
+	for (i = 0; i < count; i++)
+		listeners[i](NULL, nim);
+}
+
 static void nss_ipv4_rx_msg_handler(struct nss_ctx_instance *nss_ctx, struct nss_cmn_msg *ncm, __attribute__((unused))void *app_data)
 {
 	struct nss_ipv4_msg *nim = (struct nss_ipv4_msg *)ncm;
@@ -149,15 +196,18 @@ static void nss_ipv4_rx_msg_handler(struct nss_ctx_instance *nss_ctx, struct nss
 	/*
 	 * Do we have a callback?
 	 */
-	if (!ncm->cb) {
+	if (ncm->cb) {
+		cb = (nss_ipv4_msg_callback_t)ncm->cb;
+		cb((void *)ncm->app_data, nim);
+	}
+
+	if (nim->cm.response == NSS_CMN_RESPONSE_NOTIFY) {
+		nss_ipv4_notify_listener_dispatch(nim);
 		return;
 	}
 
-	/*
-	 * Callback
-	 */
-	cb = (nss_ipv4_msg_callback_t)ncm->cb;
-	cb((void *)ncm->app_data, nim);
+	if (nim->cm.type == NSS_IPV4_TX_CONN_STATS_SYNC_MANY_MSG)
+		nss_ipv4_conn_sync_many_listener_dispatch(nim);
 }
 
 /*
@@ -331,48 +381,130 @@ EXPORT_SYMBOL(nss_ipv4_tx_sync);
  */
 struct nss_ctx_instance *nss_ipv4_notify_register(nss_ipv4_msg_callback_t cb, void *app_data)
 {
-	/*
-	 * TODO: We need to have a new array in support of the new API
-	 * TODO: If we use a per-context array, we would move the array into nss_ctx based.
-	 */
 	nss_top_main.ipv4_callback = cb;
 	nss_top_main.ipv4_ctx = app_data;
 	return &nss_top_main.nss[nss_top_main.ipv4_handler_id];
 }
 EXPORT_SYMBOL(nss_ipv4_notify_register);
 
-/*
- * nss_ipv4_notify_unregister()
- *	Unregister to received IPv4 events.
- *
- * NOTE: Do we want to pass an nss_ctx here so that we can register for ipv4 on any core?
- */
+int nss_ipv4_notify_register_listener(nss_ipv4_msg_callback_t cb, void *app_data)
+{
+	unsigned long flags;
+	int i, free_slot = -1;
+
+	if (!cb)
+		return -EINVAL;
+
+	spin_lock_irqsave(&nss_ipv4_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV4_NOTIFY_LISTENER_MAX; i++) {
+		if ((nss_ipv4_notify_listeners[i].cb == cb) && (nss_ipv4_notify_listeners[i].app_data == app_data)) {
+			spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+			return -EEXIST;
+		}
+
+		if (!nss_ipv4_notify_listeners[i].cb && (free_slot < 0))
+			free_slot = i;
+	}
+
+	if (free_slot < 0) {
+		spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+		return -ENOSPC;
+	}
+
+	nss_ipv4_notify_listeners[free_slot].cb = cb;
+	nss_ipv4_notify_listeners[free_slot].app_data = app_data;
+	spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+	return 0;
+}
+EXPORT_SYMBOL(nss_ipv4_notify_register_listener);
+
 void nss_ipv4_notify_unregister(void)
 {
 	nss_top_main.ipv4_callback = NULL;
+	nss_top_main.ipv4_ctx = NULL;
 }
 EXPORT_SYMBOL(nss_ipv4_notify_unregister);
 
-/*
- * nss_ipv4_conn_sync_many_notify_register()
- *	Register to receive IPv4 conn_sync_many message response.
- */
+void nss_ipv4_notify_unregister_listener(nss_ipv4_msg_callback_t cb, void *app_data)
+{
+	unsigned long flags;
+	int i;
+
+	if (!cb)
+		return;
+
+	spin_lock_irqsave(&nss_ipv4_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV4_NOTIFY_LISTENER_MAX; i++) {
+		if ((nss_ipv4_notify_listeners[i].cb == cb) && (nss_ipv4_notify_listeners[i].app_data == app_data)) {
+			nss_ipv4_notify_listeners[i].cb = NULL;
+			nss_ipv4_notify_listeners[i].app_data = NULL;
+			break;
+		}
+	}
+	spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+}
+EXPORT_SYMBOL(nss_ipv4_notify_unregister_listener);
+
 void nss_ipv4_conn_sync_many_notify_register(nss_ipv4_msg_callback_t cb)
 {
 	nss_ipv4_conn_sync_many_msg_cb = cb;
 }
 EXPORT_SYMBOL(nss_ipv4_conn_sync_many_notify_register);
 
-/*
- * nss_ipv4_conn_sync_many_notify_unregister()
- *	Unregister to receive IPv4 conn_sync_many message response.
- */
+int nss_ipv4_conn_sync_many_notify_register_listener(nss_ipv4_msg_callback_t cb)
+{
+	unsigned long flags;
+	int i, free_slot = -1;
+
+	if (!cb)
+		return -EINVAL;
+
+	spin_lock_irqsave(&nss_ipv4_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV4_NOTIFY_LISTENER_MAX; i++) {
+		if (nss_ipv4_conn_sync_many_listeners[i] == cb) {
+			spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+			return -EEXIST;
+		}
+		if (!nss_ipv4_conn_sync_many_listeners[i] && (free_slot < 0))
+			free_slot = i;
+	}
+
+	if (free_slot < 0) {
+		spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+		return -ENOSPC;
+	}
+
+	nss_ipv4_conn_sync_many_listeners[free_slot] = cb;
+	spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+	return 0;
+}
+EXPORT_SYMBOL(nss_ipv4_conn_sync_many_notify_register_listener);
+
 void nss_ipv4_conn_sync_many_notify_unregister(void)
 {
 	nss_ipv4_conn_sync_many_msg_cb = NULL;
 }
 EXPORT_SYMBOL(nss_ipv4_conn_sync_many_notify_unregister);
 
+void nss_ipv4_conn_sync_many_notify_unregister_listener(nss_ipv4_msg_callback_t cb)
+{
+	unsigned long flags;
+	int i;
+
+	if (!cb)
+		return;
+
+	spin_lock_irqsave(&nss_ipv4_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV4_NOTIFY_LISTENER_MAX; i++) {
+		if (nss_ipv4_conn_sync_many_listeners[i] == cb) {
+			nss_ipv4_conn_sync_many_listeners[i] = NULL;
+			break;
+		}
+	}
+	spin_unlock_irqrestore(&nss_ipv4_notify_listener_lock, flags);
+}
+EXPORT_SYMBOL(nss_ipv4_conn_sync_many_notify_unregister_listener);
+
 /*
  * nss_ipv4_get_mgr()
  *
diff --git a/nss_ipv6.c b/nss_ipv6.c
index 17bc70e..d8f5658 100644
--- a/nss_ipv6.c
+++ b/nss_ipv6.c
@@ -57,6 +57,17 @@ static struct nss_dscp_map_entry mapping[NSS_DSCP_MAP_ARRAY_SIZE];
  */
 nss_ipv6_msg_callback_t nss_ipv6_conn_sync_many_msg_cb = NULL;
 
+#define NSS_IPV6_NOTIFY_LISTENER_MAX 8
+
+struct nss_ipv6_notify_listener {
+	nss_ipv6_msg_callback_t cb;
+	void *app_data;
+};
+
+static DEFINE_SPINLOCK(nss_ipv6_notify_listener_lock);
+static struct nss_ipv6_notify_listener nss_ipv6_notify_listeners[NSS_IPV6_NOTIFY_LISTENER_MAX];
+static nss_ipv6_msg_callback_t nss_ipv6_conn_sync_many_listeners[NSS_IPV6_NOTIFY_LISTENER_MAX];
+
 /*
  * nss_ipv6_dscp_map_usage()
  *	Help function shows the usage of the command.
@@ -74,6 +85,42 @@ static inline void nss_ipv6_dscp_map_usage(void)
  * nss_ipv6_rx_msg_handler()
  *	Handle NSS -> HLOS messages for IPv6 bridge/route
  */
+static void nss_ipv6_notify_listener_dispatch(struct nss_ipv6_msg *nim)
+{
+	struct nss_ipv6_notify_listener listeners[NSS_IPV6_NOTIFY_LISTENER_MAX];
+	unsigned long flags;
+	int i, count = 0;
+
+	spin_lock_irqsave(&nss_ipv6_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV6_NOTIFY_LISTENER_MAX; i++) {
+		if (!nss_ipv6_notify_listeners[i].cb)
+			continue;
+		listeners[count++] = nss_ipv6_notify_listeners[i];
+	}
+	spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+
+	for (i = 0; i < count; i++)
+		listeners[i].cb(listeners[i].app_data, nim);
+}
+
+static void nss_ipv6_conn_sync_many_listener_dispatch(struct nss_ipv6_msg *nim)
+{
+	nss_ipv6_msg_callback_t listeners[NSS_IPV6_NOTIFY_LISTENER_MAX];
+	unsigned long flags;
+	int i, count = 0;
+
+	spin_lock_irqsave(&nss_ipv6_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV6_NOTIFY_LISTENER_MAX; i++) {
+		if (!nss_ipv6_conn_sync_many_listeners[i])
+			continue;
+		listeners[count++] = nss_ipv6_conn_sync_many_listeners[i];
+	}
+	spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+
+	for (i = 0; i < count; i++)
+		listeners[i](NULL, nim);
+}
+
 static void nss_ipv6_rx_msg_handler(struct nss_ctx_instance *nss_ctx, struct nss_cmn_msg *ncm, __attribute__((unused))void *app_data)
 {
 	struct nss_ipv6_msg *nim = (struct nss_ipv6_msg *)ncm;
@@ -139,15 +186,18 @@ static void nss_ipv6_rx_msg_handler(struct nss_ctx_instance *nss_ctx, struct nss
 	/*
 	 * Do we have a callback?
 	 */
-	if (!ncm->cb) {
+	if (ncm->cb) {
+		cb = (nss_ipv6_msg_callback_t)ncm->cb;
+		cb((void *)ncm->app_data, nim);
+	}
+
+	if (nim->cm.response == NSS_CMN_RESPONSE_NOTIFY) {
+		nss_ipv6_notify_listener_dispatch(nim);
 		return;
 	}
 
-	/*
-	 * Callback
-	 */
-	cb = (nss_ipv6_msg_callback_t)ncm->cb;
-	cb((void *)ncm->app_data, nim);
+	if (nim->cm.type == NSS_IPV6_TX_CONN_STATS_SYNC_MANY_MSG)
+		nss_ipv6_conn_sync_many_listener_dispatch(nim);
 }
 
 /*
@@ -321,48 +371,129 @@ EXPORT_SYMBOL(nss_ipv6_tx_sync);
  */
 struct nss_ctx_instance *nss_ipv6_notify_register(nss_ipv6_msg_callback_t cb, void *app_data)
 {
-	/*
-	 * TODO: We need to have a new array in support of the new API
-	 * TODO: If we use a per-context array, we would move the array into nss_ctx based.
-	 */
 	nss_top_main.ipv6_callback = cb;
 	nss_top_main.ipv6_ctx = app_data;
 	return &nss_top_main.nss[nss_top_main.ipv6_handler_id];
 }
 EXPORT_SYMBOL(nss_ipv6_notify_register);
 
-/*
- * nss_ipv6_notify_unregister()
- *	Unregister to received IPv6 events.
- *
- * NOTE: Do we want to pass an nss_ctx here so that we can register for ipv6 on any core?
- */
+int nss_ipv6_notify_register_listener(nss_ipv6_msg_callback_t cb, void *app_data)
+{
+	unsigned long flags;
+	int i, free_slot = -1;
+
+	if (!cb)
+		return -EINVAL;
+
+	spin_lock_irqsave(&nss_ipv6_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV6_NOTIFY_LISTENER_MAX; i++) {
+		if ((nss_ipv6_notify_listeners[i].cb == cb) && (nss_ipv6_notify_listeners[i].app_data == app_data)) {
+			spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+			return -EEXIST;
+		}
+		if (!nss_ipv6_notify_listeners[i].cb && (free_slot < 0))
+			free_slot = i;
+	}
+
+	if (free_slot < 0) {
+		spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+		return -ENOSPC;
+	}
+
+	nss_ipv6_notify_listeners[free_slot].cb = cb;
+	nss_ipv6_notify_listeners[free_slot].app_data = app_data;
+	spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+	return 0;
+}
+EXPORT_SYMBOL(nss_ipv6_notify_register_listener);
+
 void nss_ipv6_notify_unregister(void)
 {
 	nss_top_main.ipv6_callback = NULL;
+	nss_top_main.ipv6_ctx = NULL;
 }
 EXPORT_SYMBOL(nss_ipv6_notify_unregister);
 
-/*
- * nss_ipv6_conn_sync_many_notify_register()
- *	Register to receive IPv6 conn_sync_many message response.
- */
+void nss_ipv6_notify_unregister_listener(nss_ipv6_msg_callback_t cb, void *app_data)
+{
+	unsigned long flags;
+	int i;
+
+	if (!cb)
+		return;
+
+	spin_lock_irqsave(&nss_ipv6_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV6_NOTIFY_LISTENER_MAX; i++) {
+		if ((nss_ipv6_notify_listeners[i].cb == cb) && (nss_ipv6_notify_listeners[i].app_data == app_data)) {
+			nss_ipv6_notify_listeners[i].cb = NULL;
+			nss_ipv6_notify_listeners[i].app_data = NULL;
+			break;
+		}
+	}
+	spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+}
+EXPORT_SYMBOL(nss_ipv6_notify_unregister_listener);
+
 void nss_ipv6_conn_sync_many_notify_register(nss_ipv6_msg_callback_t cb)
 {
 	nss_ipv6_conn_sync_many_msg_cb = cb;
 }
 EXPORT_SYMBOL(nss_ipv6_conn_sync_many_notify_register);
 
-/*
- * nss_ipv6_conn_sync_many_notify_unregister()
- *	Unregister to receive IPv6 conn_sync_many message response.
- */
+int nss_ipv6_conn_sync_many_notify_register_listener(nss_ipv6_msg_callback_t cb)
+{
+	unsigned long flags;
+	int i, free_slot = -1;
+
+	if (!cb)
+		return -EINVAL;
+
+	spin_lock_irqsave(&nss_ipv6_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV6_NOTIFY_LISTENER_MAX; i++) {
+		if (nss_ipv6_conn_sync_many_listeners[i] == cb) {
+			spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+			return -EEXIST;
+		}
+		if (!nss_ipv6_conn_sync_many_listeners[i] && (free_slot < 0))
+			free_slot = i;
+	}
+
+	if (free_slot < 0) {
+		spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+		return -ENOSPC;
+	}
+
+	nss_ipv6_conn_sync_many_listeners[free_slot] = cb;
+	spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+	return 0;
+}
+EXPORT_SYMBOL(nss_ipv6_conn_sync_many_notify_register_listener);
+
 void nss_ipv6_conn_sync_many_notify_unregister(void)
 {
 	nss_ipv6_conn_sync_many_msg_cb = NULL;
 }
 EXPORT_SYMBOL(nss_ipv6_conn_sync_many_notify_unregister);
 
+void nss_ipv6_conn_sync_many_notify_unregister_listener(nss_ipv6_msg_callback_t cb)
+{
+	unsigned long flags;
+	int i;
+
+	if (!cb)
+		return;
+
+	spin_lock_irqsave(&nss_ipv6_notify_listener_lock, flags);
+	for (i = 0; i < NSS_IPV6_NOTIFY_LISTENER_MAX; i++) {
+		if (nss_ipv6_conn_sync_many_listeners[i] == cb) {
+			nss_ipv6_conn_sync_many_listeners[i] = NULL;
+			break;
+		}
+	}
+	spin_unlock_irqrestore(&nss_ipv6_notify_listener_lock, flags);
+}
+EXPORT_SYMBOL(nss_ipv6_conn_sync_many_notify_unregister_listener);
+
 /*
  * nss_ipv6_get_mgr()
  *
